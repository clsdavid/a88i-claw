---
summary: "CLI reference for `autocrab clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `autocrab clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `autocrab clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `autocrab clawbot qr` (same behavior as [`autocrab qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `autocrab clawbot qr` -> `autocrab qr`
