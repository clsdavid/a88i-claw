---
summary: "CLI reference for `autocrab uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `autocrab uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
autocrab uninstall
autocrab uninstall --all --yes
autocrab uninstall --dry-run
```
